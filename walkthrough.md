# คู่มือการติดตั้งระบบจองโต๊ะ (LINE Login + Staff Portal)

ระบบถูกออกแบบใหม่โดยแบ่งออกเป็น 2 ส่วนหลักคือ **หน้าระบบลูกค้า (index.html)** ที่เชื่อมต่อกับ LINE Login และ **ระบบหลังบ้านพนักงาน (StaffUI.html)** ที่อยู่บน Google Apps Script โดยตรง

เพื่อให้ระบบทำงานได้สมบูรณ์ กรุณาทำตามขั้นตอนด้านล่างนี้ครับ

---

## ส่วนที่ 1: การตั้งค่า LINE Login (LIFF) สำหรับลูกค้า

เราจะใช้ LINE LIFF เพื่อดึงชื่อและรูปโปรไฟล์ลูกค้ามาอัตโนมัติ

1. ไปที่เว็บไซต์ **[LINE Developers](https://developers.line.biz/)** แล้วล็อกอินด้วยบัญชี LINE ของคุณ
2. สร้าง **Provider** ใหม่ (เช่น ตั้งชื่อว่า VIP Booking)
3. กดสร้าง **Channel** เลือกประเภทเป็น **LINE Login**
4. เมื่อสร้างเสร็จ ให้ไปที่แท็บ **LIFF** แล้วกด **Add**
   - **Size:** เลือก Full
   - **Endpoint URL:** ใส่ URL หน้าเว็บ `index.html` ของคุณ (อ่านส่วนที่ 3 เรื่องการทำ Hosting เพื่อเอา URL มาใส่ตรงนี้)
   - **Scopes:** ติ๊กเลือก `profile` และ `openid`
5. กด Add แล้วคุณจะได้ **LIFF ID** (ลักษณะเป็นตัวเลขและภาษาอังกฤษ เช่น `1234567890-AbcdEfgh`)
6. นำ LIFF ID นั้น มาใส่ในไฟล์ `index.html` บรรทัดที่ **591**:
   ```javascript
   const LIFF_ID = "ใส่_LIFF_ID_ของคุณที่นี่"; 
   ```

---

## ส่วนที่ 2: การติดตั้ง Google Apps Script แบบ 2 โหมด

โค้ดชุดใหม่นี้รองรับทั้ง API สำหรับลูกค้า และหน้าจอ Dashboard สำหรับพนักงานในไฟล์เดียว!

1. เปิด Google Sheet ที่คุณใช้เก็บข้อมูลจองโต๊ะ
2. ไปที่เมนู **ส่วนขยาย (Extensions) > Apps Script**
3. ลบโค้ดเก่าทิ้งทั้งหมด แล้วนำโค้ดจากไฟล์ `apps_script.js` (ที่ผมเขียนให้ใหม่) ไปวางใน `Code.gs`
   - *หมายเหตุ: ตรงบรรทัดที่ 7 คุณสามารถใส่ Gmail ของพนักงานที่อนุญาตให้เข้าถึงระบบได้ (ถ้าปล่อยว่างไว้ พนักงานทุกคนที่มีลิงก์จะเข้าได้)*
4. กดที่ปุ่ม **+ (เพิ่มไฟล์)** ด้านซ้ายมือ เลือก **HTML**
5. ตั้งชื่อไฟล์ว่า `StaffUI` (ต้องชื่อนี้เป๊ะๆ ไม่มี .html ต่อท้ายตอนตั้งชื่อ)
6. นำโค้ดทั้งหมดจากไฟล์ `StaffUI.html` (ที่ผมเขียนให้ใหม่) ไปวางทับในไฟล์ StaffUI นั้น
7. กด **บันทึก** (ไอคอนแผ่นดิสก์)

### การ Deploy นำไปใช้งาน (สำคัญมาก ⚠️)

เพื่อให้ทั้งลูกค้าและพนักงานใช้งานได้ คุณต้อง Deploy **1 ครั้ง** โดยตั้งค่าดังนี้:

1. กดปุ่ม **การทำให้ใช้งานได้ (Deploy) > การทำให้ใช้งานได้รายการใหม่ (New deployment)**
2. เลือกประเภทเป็น **เว็บแอป (Web App)**
3. ตั้งค่าการเข้าถึง:
   - **เรียกใช้งานในฐานะ (Execute as):** เลือก **ผู้ที่เข้าถึงเว็บแอป (User accessing the web app)** 
   - **ผู้มีสิทธิ์เข้าถึง (Who has access):** เลือก **ทุกคนที่มีบัญชี Google (Anyone with Google account)**
4. กด Deploy แล้วคัดลอก **URL เว็บแอป** มา

**นำ URL ไปใช้งาน 2 ที่ดังนี้:**
- **สำหรับลูกค้า:** นำ URL นั้นไปวางในไฟล์ `index.html` บรรทัดที่ **577** (ตรงตัวแปร `SCRIPT_URL`)
- **สำหรับพนักงาน:** ส่ง URL นั้นให้พนักงานเซฟเก็บไว้ พนักงานเปิดลิงก์นี้ในมือถือหรือคอม ระบบจะให้ล็อกอิน Google แล้วจะพาเข้าสู่หน้า Dashboard จัดการโต๊ะทันที

---

## ส่วนที่ 3: การนำเว็บขึ้นออนไลน์ (Hosting)

ไฟล์ `index.html` ปัจจุบันอยู่ในเครื่องของคุณ ลูกค้าจะเข้าไม่ได้ คุณต้องนำไปฝากบนเน็ต ผมแนะนำบริการฟรีที่ง่ายที่สุดคือ **Vercel**

1. สมัครใช้งานที่ [Vercel.com](https://vercel.com/) (ล็อกอินด้วย GitHub หรือ Google)
2. เมื่อเข้ามาที่ Dashboard ให้ลากไฟล์ `index.html` (รวมถึงโฟลเดอร์รูปภาพถ้ามี) ไปวางในช่อง Drop files
3. รอสักครู่ ระบบจะสร้าง URL มาให้คุณฟรี เช่น `https://your-project.vercel.app`
4. นำ URL นี้แหละ ไปกรอกในช่อง **Endpoint URL** ของการตั้งค่า LINE LIFF ในส่วนที่ 1

> [!TIP]
> เมื่อตั้งค่าครบทุกอย่างแล้ว ลูกค้าจะสามารถจองโต๊ะผ่านลิงก์ LIFF ได้อย่างปลอดภัย และพนักงานก็จะมีระบบหลังบ้านส่วนตัวที่ล็อกอินผ่าน Google ครับ
